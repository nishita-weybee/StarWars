import Header from "../../common/header";
import Card from "../../common/Card";

const Starships = () => {
  return (
    <div className="container">
      <Header header="Starships" />
      <Card cardData={""} />
    </div>
  );
};

export default Starships;
